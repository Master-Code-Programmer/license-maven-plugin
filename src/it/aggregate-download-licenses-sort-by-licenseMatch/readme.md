See a HTML table of `sortedByLicenseMatch.xml` by starting a local webserver in this directory, like:

```shell
python3 -m http.server
```

Then open it in a web-browser on http://localhost:8000/sortedByLicenseMatch.xml.
Otherwise, the necessary XSLT is blocked by all modern web browsers for security reasons.
